Drawing Canvas:

Smooth drawing with mouse or touch input
Works properly on mobile devices
Color Selection:

8 predefined colors plus custom color picker
Color preview system
Brush Controls:

Adjustable brush size (1-50px)
Visual brush size preview
Tools:

Clear canvas button
Eraser tool (sets color to white)
Download drawing as PNG image
Responsive Design:

Adapts to different screen sizes
Clean, intuitive interface
Everything is contained in a single HTML file with embedded CSS and JavaScript, with no external dependencies. The canvas automatically adjusts to its container size and maintains proper drawing proportions.
